# Planner Extractors Sub-package
#
# Strategy + Registry pattern for intent-specific parameter extraction.
# Adding a new intent extractor requires ONLY:
#   1. Add a new extractor file in this package
#   2. Register it in registry.py
#
# Zero changes to planner.py dispatch logic.
